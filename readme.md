# Telecom Churn Predictor

AI-powered churn prediction using XGBoost + FastAPI + modern UI.

## Local Setup
1. Put `telecom_churn_xgb_strong.joblib` in this folder
2. `pip install -r requirements.txt`
3. `uvicorn app:app --reload --port 8000`
4. Open `index.html` in browser
